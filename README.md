# 🦴 Skeleton CLI

> A blazing-fast CLI tool to **set up Nuxt 4 projects instantly** — with preconfigured linters, SCSS setup, VS Code settings, and best-practice project structure.

Skeleton helps you **save 20–30% of setup time** every time you start a new Nuxt project. No more copying configs or re-installing the same tools — just one command and you’re ready to code.

---

## Installation

Currently tested only in macOS.

```bash
curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/cli-labs/skeleton/main/install.sh | sh
```

## 🚀 Features

### ✅ Instant Project Bootstrapping
- Create a fully configured Nuxt 4 project in seconds.  
- Automatically initializes project folders, config files, and VS Code workspace settings.

### ✅ Prebuilt Configuration Templates
- ESLint + Prettier preconfigured for Nuxt 4.
- SCSS structure with `variables`, `mixins`, and base styles.
- `.vscode/settings.json` with recommended editor settings.

### Expected Output

Under `hacktoberfest` contest, any contributer can build this and raise an MR.

```bash
skeleton init
```

Post that it shows the framework options. If only one option is there then it auto-selects it.
